{
  "name": "projectapp-functions",
  "description": "Cloud Functions for ProjectApp admin features",
  "engines": {
    "node": "18"
  },
  "scripts": {
    "lint": "eslint --ext .js .",
    "build": "echo 'no build step'",
    "deploy": "echo 'use firebase deploy --only functions'"
  },
  "dependencies": {
    "firebase-admin": "^11.11.1",
    "firebase-functions": "^4.9.0",
    "stripe": "^14.0.0"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "eslint-config-google": "^0.14.0"
  }
}
